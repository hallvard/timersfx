image:
  file: .gitpod.dockerfile

tasks:
  - init: |
      cd no.hal.timers
      mvn install
      echo "run 'cd fxui.android; mvn client:build client:package' to build apk"

ports:
  # used by virtual desktop and vnc, supports JavaFX
  - port: 6080
